package spring.test1;

public class BeanDefinitionTest {
/**
 * Spring通过BeanDefinition来管理我们基于Spring应用中各种对象和他们的依赖关系，BeanDefinition抽象了我们对Bean的定义。是让容器起作用的
 * 		重要数据类型，Ioc是用来管理对象依赖关系的，对IOC容器来说，BeanDefinition是核心数据结构，依赖反转是通过BeanDefinition对处理来完成。
 * 		这些 BeanDefinition 就像是容器里装满了水，有了这些基本数据，容器才会发生作用
 */
}
